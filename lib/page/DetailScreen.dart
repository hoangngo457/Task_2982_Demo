import 'package:flutter/cupertino.dart';

class RoomDetail extends StatelessWidget {
  const RoomDetail({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      child: Text("This is room detail!"),
    );
  }
}
